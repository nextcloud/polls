build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run

filter:
    excluded_paths:
        - 'build/*'
        - 'l10n/*'
        - 'tests/'
        - 'vendor/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage: false
