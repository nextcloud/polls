filter:
      excluded_paths:
        - 'l10n/*'
        - 'js/vendor/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage: false

build:
    tests:
        override:
          -  jshint-run --config .jshintrc
