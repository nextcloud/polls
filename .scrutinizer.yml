filter:
      excluded_paths:
        - 'l10n/*'
        - 'js/vendor/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage: false

build:
    environment:
        node: 14.15.0
    tests:
        override:
          -  jshint-run --config .jshintrc
